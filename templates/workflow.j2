---
title: {{ wf.key }}
description: "{{ wf.summary }}"
tags:
{{ tags_yaml(tags) }}
---

# {{ wf.key }}

{{ badges }}

{{ description }}

{% block after_description %}{% endblock %}
{% if wf.type == 'tool' %}
## Usage

```bash
bactopia --wf {{ wf.key }} \
  --bactopia /path/to/your/bactopia/results
```
{% else %}
## Usage

```bash
bactopia --wf {{ wf.key }} \
  --input samples.csv \
  --outdir results/
```
{% endif %}

{% block after_usage %}{% endblock %}
{% if outputs_section %}
{{ outputs_section }}
{% endif %}

### Audit Trail

Below are files that can assist you in understanding which parameters and program versions were used.

#### Logs

Each process that is executed will have a folder named `logs`. In this folder are helpful
files for you to review if the need ever arises.

| Extension    | Description |
|--------------|-------------|
| .begin       | An empty file used to designate the process started |
| .err         | Contains STDERR outputs from the process |
| .log         | Contains both STDERR and STDOUT outputs from the process |
| .out         | Contains STDOUT outputs from the process |
| .run         | The script Nextflow uses to stage/unstage files and queue processes based on given profile |
| .sh          | The script executed by bash for the process  |
| .trace       | The Nextflow [Trace](https://www.nextflow.io/docs/latest/tracing.html#trace-report) report for the process |
| versions.yml | A YAML formatted file with program versions |

#### Nextflow Reports

These Nextflow reports provide great a great summary of your run. These can be used to optimize
resource usage and estimate expected costs if using cloud platforms.

| Filename | Description |
|----------|-------------|
| {{ wf.key }}-dag.dot | The Nextflow [DAG visualisation](https://docs.seqera.io/nextflow/reports#dag-visualisation) |
| {{ wf.key }}-report.html | The Nextflow [Execution Report](https://docs.seqera.io/nextflow/reports#execution-report) |
| {{ wf.key }}-timeline.html | The Nextflow [Timeline Report](https://docs.seqera.io/nextflow/reports#timeline-report) |
| {{ wf.key }}-trace.txt | The Nextflow [Trace](https://docs.seqera.io/nextflow/reports#trace-report) report |

{% block after_outputs %}{% endblock %}
{% if params_section %}
{{ params_section }}
{% endif %}
{% block after_params %}{% endblock %}
{% if composition_section %}
{{ composition_section }}
{% endif %}
{% block before_citations %}{% endblock %}
{% if citations_section %}
{{ citations_section }}
{% endif %}
## Source

[View source on GitHub]({{ wf.source_url }})
